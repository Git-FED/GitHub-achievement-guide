# Installation

Clone the repository, open `index.html` directly for a simple preview, or serve the directory with any static HTTP server. PDF generation requires the provided `manus-md-to-pdf` command.
