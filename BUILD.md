# Build

Run `./scripts/build-site.sh` to copy the static site into `site-dist/`. Run `./scripts/generate-pdfs.sh` to create printable downloads and `./scripts/package-downloads.sh` to package the resource folder.
